% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clocks_api.R
\name{DNAmAge}
\alias{DNAmAge}
\title{Estimate chronological and biological DNAm age}
\usage{
DNAmAge(
  x,
  clocks = "all",
  generation = NULL,
  platform = NULL,
  min.perc = 0.8,
  storage = "auto",
  ...
)
}
\arguments{
\item{x}{A methylation matrix with CpG identifiers as row names and samples
as columns (a data frame with a CpG-identifier column is also accepted), or
a large matrix held on disk as an HDF5-backed matrix.}

\item{clocks}{Either \code{"all"} (default) or a character vector of clock
names; see \code{\link{clock_list}}.}

\item{generation}{Optional generation to restrict to (e.g. \code{1}).}

\item{platform}{Optional native platform to restrict to (e.g. \code{"450K"}),
handy when the data come from a known array and only the clocks built for
it should run.}

\item{min.perc}{Minimum fraction of a clock's CpGs that must be present for
it to be computed (otherwise \code{NA}). Default \code{0.8}.}

\item{storage}{How the estimates are held: \code{"auto"} (default) backs them
on disk (HDF5) with an in-memory cache when they fit, \code{"hdf5"} on disk
only, \code{"memory"} in memory only (not persisted). See
\code{\link{compute_clocks}} for the \code{file} and \code{compression}
arguments, and \code{\link{persist}} to keep a session result.}

\item{...}{Passed to \code{\link{compute_clocks}} (e.g. \code{file},
\code{compression}).}
}
\value{
A \code{"methylclock"} object.
}
\description{
Convenience wrapper around \code{\link{methylclock}} restricted to
non-gestational clocks.
}
\examples{
data(methylclock_betas)
head(as.data.frame(DNAmAge(methylclock_betas, clocks = "Horvath")))
}
\seealso{
\code{\link{methylclock}}, \code{\link{DNAmGA}}
}
