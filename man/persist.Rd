% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/persist.R
\name{persist}
\alias{persist}
\alias{persist.methylclock}
\title{Persist a result to a file}
\usage{
persist(x, file, ...)

\method{persist}{methylclock}(x, file, overwrite = FALSE, ...)
}
\arguments{
\item{x}{The object to persist.}

\item{file}{Destination file path.}

\item{...}{Further arguments passed to methods.}

\item{overwrite}{Overwrite \code{file} if it already exists. Default
\code{FALSE}.}
}
\value{
The object, updated to reference the persistent file (invisibly).
}
\description{
Save an object to a file so it outlives the current session.
}
\details{
For a \code{"methylclock"} result the estimates already live in an
  HDF5 file; when that file is a session temporary, persisting copies it to
  \code{file} (no recomputation) and verifies the copy. A result held only in
  memory is written out to \code{file}. The returned object references the
  persistent file.
}
\examples{
data(methylclock_betas)
res <- methylclock(methylclock_betas, clocks = "Horvath")
f <- tempfile(fileext = ".h5")
res <- persist(res, f)
file.exists(f)
unlink(f)
}
