% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clock_registry.R
\name{clock_register}
\alias{clock_register}
\title{Register a clock}
\usage{
clock_register(
  name,
  target,
  predictor = "linear",
  generation = NA_integer_,
  platform = character(0),
  compatible = character(0),
  tissue = NA_character_,
  resource = NULL,
  aux = NULL,
  coef_col = "CoefficientTraining",
  intercept = TRUE,
  standardize = FALSE,
  normalize = NA_character_,
  agg = "mean",
  transform = identity,
  inputs = character(0),
  feature_type = "cpg",
  output_type = "numeric",
  license = NA_character_,
  source = NA_character_,
  citation = NA_character_,
  status = "ok",
  notes = NA_character_
)
}
\arguments{
\item{name}{Clock name (unique key), e.g. \code{"Horvath"}.}

\item{target}{Biological target; one of \code{\link{clock_targets}}.}

\item{predictor}{Prediction engine; one of \code{\link{clock_predictors}}.
Defaults to \code{"linear"}.}

\item{generation}{Clock generation (e.g. 1, 2, 3) or \code{NA}.}

\item{platform}{Character vector of the clock's native arrays --- the
platform(s) it was built and validated on, e.g. \code{c("450K", "EPIC")}.}

\item{compatible}{Character vector of additional arrays the clock can be used
with through probe conversion or adaptation, i.e. not its native platform.}

\item{tissue}{The tissue the clock was trained on (e.g. \code{"whole
blood"}, \code{"buccal epithelium (children)"}, \code{"placenta"},
\code{"multi-tissue"}): a clock reads best the tissue it came from.
Empty by default. Selection and estimation treat these as non-native.}

\item{resource}{Resource identifier (resolved by \code{\link{mcd_resource}})
holding the coefficients, e.g. \code{"coefHorvath"}.}

\item{aux}{Character vector of auxiliary resource identifiers (e.g.
normalization means, a PCA rotation, a cell-type reference), or \code{NULL}.}

\item{coef_col}{Name of the coefficient column in the resource. Default
\code{"CoefficientTraining"}.}

\item{intercept}{Logical: is the first coefficient row the intercept?}

\item{standardize}{Logical: z-score each sample across the CpGs of the input
matrix before applying the coefficients (as some elastic-net clocks
require). Default \code{FALSE}.}

\item{normalize}{Optional input normalization applied before the
coefficients. \code{"quantile_gs"} quantile-normalizes each sample to a
gold-standard reference distribution taken from the clock's \code{aux}
resource. Default \code{NA} (none).}

\item{agg}{For \code{"counter"} clocks, how the CpG set is aggregated per
sample: \code{"mean"} (the default) or \code{list(fun = "quantile", p =
<0..1>)} for an upper-quantile summary. Ignored by other engines.}

\item{transform}{Function applied to the raw prediction (e.g. an
anti-transform). Default \code{identity}.}

\item{inputs}{Character vector of inputs required beyond the beta matrix
(e.g. \code{c("age", "sex")}). Default none.}

\item{feature_type}{Feature space of the predictor. Default \code{"cpg"};
kept configurable so the same engine can operate on non-CpG features.}

\item{output_type}{One of \code{"numeric"}, \code{"probability"},
\code{"category"}. Default \code{"numeric"}.}

\item{license}{Short license/provenance tag for the weights. Default
\code{NA}.}

\item{source}{Where the weights come from (URL, package, or resource id).}

\item{citation}{Short citation key.}

\item{status}{Availability status; one of \code{\link{clock_statuses}}.
Default \code{"ok"}.}

\item{notes}{Free-text notes.}
}
\value{
Invisibly, the registry entry (a list of class \code{clock_entry}).
}
\description{
Adds (or replaces) one clock in the package registry. A registration is a
single declarative description of a clock; the estimation engine reads it to
compute the clock's value.
}
\examples{
clock_register("MyClock", target = "chronological", resource = "coefMyClock")
clock_info("MyClock")
}
\seealso{
\code{\link{clock_list}}, \code{\link{clock_info}},
  \code{\link{mcd_resource}}
}
