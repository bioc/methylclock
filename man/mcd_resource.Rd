% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcd_resource.R
\name{mcd_resource}
\alias{mcd_resource}
\title{Resolve a clock resource by identifier}
\usage{
mcd_resource(id, verify = FALSE, cache = TRUE)
}
\arguments{
\item{id}{Resource identifier, e.g. \code{"coefHorvath"} (a row of
\code{\link{mcd_manifest}}).}

\item{verify}{Logical; verify the object's checksum when possible. Default
\code{FALSE}.}

\item{cache}{Logical; use and populate the private cache. Default
\code{TRUE}.}
}
\value{
The resource object in canonical form (see \code{\link{normalize_coef}}).
}
\description{
The single access point for coefficient objects. Resolves \code{id} through
the backend chain (see \code{\link{mcd_backends}}), caches the result in a
private environment and returns it.
}
\examples{
coef <- mcd_resource("coefHorvath")
head(coef)
}
\seealso{
\code{\link{mcd_manifest}}, \code{\link{mcd_backends}},
  \code{\link{clock_register}}
}
