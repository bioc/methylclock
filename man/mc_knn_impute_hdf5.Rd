% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{mc_knn_impute_hdf5}
\alias{mc_knn_impute_hdf5}
\title{KNN-impute a matrix stored in HDF5 (reads through BigDataStatMeth)}
\usage{
mc_knn_impute_hdf5(
  filename,
  group,
  dataset,
  k = 10L,
  maxp = 1500L,
  rowmax = 0.5,
  colmax = 0.8
)
}
\arguments{
\item{filename}{HDF5 file path.}

\item{group}{Group holding the dataset.}

\item{dataset}{Dataset name (CpGs x samples in R view).}

\item{k, maxp, rowmax, colmax}{See \code{mc_knn_impute_mem}.}
}
\value{
The filled matrix (CpGs x samples), without dimnames.
}
\description{
Reads a CpGs x samples matrix from an HDF5 dataset in R-view order through
the BigDataStatMeth C++ API (so the R/HDF5 layout is reconciled by the
library), imputes it with the same engine as \code{mc_knn_impute_mem}, and
returns the filled matrix. This validates the HDF5 read path against the
in-memory one; the memory-bounded block-wise variant builds on it.
}
\keyword{internal}
