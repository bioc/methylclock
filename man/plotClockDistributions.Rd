% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_distributions.R
\name{plotClockDistributions}
\alias{plotClockDistributions}
\title{Plot the distribution of each clock's estimates}
\usage{
plotClockDistributions(
  x,
  clocks = NULL,
  scale = FALSE,
  ncol = 4,
  color_by = c("auto", "target", "none")
)
}
\arguments{
\item{x}{A \code{methylclock} result, or a data frame with an \code{id}
column and one numeric column per clock.}

\item{clocks}{Optional character vector of clock names. Default: all numeric
clocks in \code{x}.}

\item{scale}{z-score each clock onto one axis (\code{TRUE}) or facet each
on its own scale (\code{FALSE}, the default).}

\item{ncol}{Facet columns when \code{scale = FALSE}. Default 4.}

\item{color_by}{Colour the violins by clock family (its registry
\code{target}): \code{"auto"} (the default) colours only when more than one
family is shown, \code{"target"} always, \code{"none"} never.}
}
\value{
A \code{ggplot} object.
}
\description{
A violin plus box of every clock's per-sample values, to see the spread and
any skew or outliers at a glance. Because clocks live on different scales
(years, counts, scores), \code{scale = TRUE} z-scores each clock to
one axis; \code{scale = FALSE} facets them on their own scales.
}
\examples{
data(methylclock_demo)
plotClockDistributions(methylclock_demo,
                       clocks = c("Horvath", "Hannum", "Levine"))
}
