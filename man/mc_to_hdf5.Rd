% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{mc_to_hdf5}
\alias{mc_to_hdf5}
\title{Store a methylation matrix on disk as HDF5}
\usage{
mc_to_hdf5(
  x,
  file = NULL,
  overwrite = FALSE,
  sep = NULL,
  header = TRUE,
  row.names = TRUE,
  compression = NULL
)
}
\arguments{
\item{x}{A methylation matrix or data frame (CpG identifiers as row names / a
CpG-identifier column, samples as columns), or the path to a delimited text
file laid out the same way. A text file is imported without being loaded
into memory first.}

\item{file}{Destination HDF5 file. Pass a real path to keep it; a session
temporary file is used when \code{NULL} (default).}

\item{overwrite}{Overwrite \code{file} if it exists. Default \code{FALSE}.}

\item{sep}{For a text file, the field separator (default: auto by extension,
comma for \code{.csv} else tab).}

\item{header, row.names}{For a text file, whether it has a header row / a
leading column of CpG identifiers. Both default to \code{TRUE}.}

\item{compression}{HDF5 compression level (0-9); \code{NULL} (default) picks
it by size.}
}
\value{
An on-disk (HDF5-backed) matrix, open for reading; pass it to
  \code{\link{methylclock}} and \code{close()} it when done.
}
\description{
Writes a methylation matrix to an HDF5 file and returns it as an on-disk
matrix that \code{\link{methylclock}} can estimate block by block, without
holding the whole array in memory. Useful for arrays too large to work with
comfortably in RAM: convert once, then pass the result (and let the in-memory
copy go).
}
\examples{
data(methylclock_betas)
f <- tempfile(fileext = ".h5")
hm <- mc_to_hdf5(methylclock_betas, f)
res <- methylclock(hm, clocks = "Horvath")
head(as.data.frame(res))
close(hm)
unlink(f)
}
\seealso{
\code{\link{methylclock}}
}
