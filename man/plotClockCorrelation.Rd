% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_clock_correlation.R
\name{plotClockCorrelation}
\alias{plotClockCorrelation}
\title{Heatmap of the correlation between clocks}
\usage{
plotClockCorrelation(x, clocks = NULL, method = "pearson", cluster = TRUE)
}
\arguments{
\item{x}{A \code{methylclock} result, or a data frame with an \code{id}
column and one numeric column per clock.}

\item{clocks}{Optional character vector of clock names to include. Default:
all numeric clocks in \code{x}.}

\item{method}{Correlation method passed to \code{\link[stats]{cor}}
(\code{"pearson"}, \code{"spearman"} or \code{"kendall"}). Default Pearson.}

\item{cluster}{Reorder clocks by hierarchical clustering so correlated ones
sit together. Default \code{TRUE}.}
}
\value{
A \code{ggplot} object.
}
\description{
Correlation of the per-sample estimates across every pair of clocks, as a
heatmap. Clocks that measure similar things correlate highly; this is the
clearest view of how much the clocks agree (and where they are redundant).
}
\examples{
data(methylclock_demo)
plotClockCorrelation(methylclock_demo,
                     clocks = c("Horvath", "Hannum", "Levine", "TL"))
}
