% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{mc_knn_impute_mem}
\alias{mc_knn_impute_mem}
\title{KNN-impute a matrix in memory (C++/Eigen, block split via BigDataStatMeth)}
\usage{
mc_knn_impute_mem(Bin, k = 10L, maxp = 1500L, rowmax = 0.5, colmax = 0.8)
}
\arguments{
\item{Bin}{Numeric matrix (CpGs x samples); missing values are \code{NA}.}

\item{k}{Number of neighbours averaged for each imputed value.}

\item{maxp}{Largest block imputed exactly; larger row sets are split first.}

\item{rowmax}{A row with a greater missing fraction than this is filled with
the per-column mean instead of by KNN.}

\item{colmax}{A column with a greater missing fraction than this is flagged
(a warning is raised in R); imputation still proceeds.}
}
\value{
The matrix with missing values filled.
}
\description{
Fills the missing entries of a CpGs x samples matrix from each CpG's k
nearest co-varying CpGs. A row set within \code{maxp} is imputed exactly
(every CpG can be a neighbour of every other); a larger one is first split
into blocks of similar CpGs through a BigDataStatMeth SVD embedding and
2-means, then each block is imputed exactly.
}
\keyword{internal}
