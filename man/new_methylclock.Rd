% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methylclock_class.R
\name{new_methylclock}
\alias{new_methylclock}
\title{Construct a methylclock result}
\usage{
new_methylclock(
  values,
  clocks,
  storage = "memory",
  handle = NULL,
  persistent = FALSE,
  meta = list()
)
}
\arguments{
\item{values}{A data frame of estimates (one row per sample, one column per
clock) when it is held in memory; \code{NULL} when the estimates live
only on disk and are reached through \code{handle}.}

\item{clocks}{Character vector of the clocks that were computed.}

\item{storage}{Where the authoritative estimates live: \code{"memory"} or
\code{"hdf5"}.}

\item{handle}{For \code{storage = "hdf5"}, a descriptor of the on-disk
dataset (file, group, dataset); \code{NULL} otherwise.}

\item{persistent}{\code{TRUE} if the on-disk file is meant to outlive the
session (a user-supplied path); \code{FALSE} for a session temporary file.}

\item{meta}{Named list of metadata (e.g. sample count, call, warnings).}
}
\value{
An object of class \code{"methylclock"}.
}
\description{
Low-level constructor for the object returned by the clock estimation
functions. Most users obtain one from \code{\link{methylclock}},
\code{\link{DNAmAge}} or \code{\link{DNAmGA}} rather than calling this
directly.
}
\examples{
est <- data.frame(id = c("s1", "s2"), Horvath = c(41.2, 55.8))
res <- new_methylclock(est, clocks = "Horvath")
as.data.frame(res)
}
\seealso{
\code{\link{methylclock}}
}
